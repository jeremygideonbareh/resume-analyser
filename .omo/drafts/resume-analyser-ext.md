# resume-analyser-ext — draft (durable resume point)

## Status
- **intent:** clear
- **review_required:** false (no review modifier requested) — high-accuracy review (dual Momus+Oracle) REQUESTED by user, then superseded by scope change
- **status:** APPROVED by user ("yes" on 2026-08-14) → plan written `.omo/plans/resume-analyser-ext.md`; **RE-SCOPED 2026-08-14** per user's Supabase request + two question answers
- **RE-SCOPE (user decision, 2026-08-14):** user supplied a Supabase Personal Access Token (`sbp_…`, Management API) and said "use this supabase token to set up everything needed for backend". Asked two forks; user chose **"Real auth, keep privacy-first (Recommended)"** + **"Provision new project (Recommended)"**. → Wave 2 rewritten from in-memory demo login to REAL Supabase Auth: provision free-tier project via Management API (PowerShell Invoke-RestMethod, no CLI), wire `@supabase/supabase-js`, email OTP live (built-in emailer) + phone OTP wired with graceful SMS-provider-missing error (paid provider required for real delivery), session via `onAuthStateChange` persisted with Supabase token, privacy copy updated to stay truthful. Security policy: PAT + service_role + DB password live ONLY in gitignored `.env.local`; rotate PAT after setup (shared in chat); secret-leak grep gate every todo.
- **RE-SCOPE #2 (user decision, 2026-08-14):** user said "now make sure every user when logged in has a personal dashboard where they can access all features we provide and they can access all their data visualized" + supplied the `efferd-dashboard-2` shadcn block integration snippet. Asked two forks; user chose **"Store per-user history in Supabase"** + **"Install + re-theme to resume domain (Recommended)"**. → NEW Wave 3 (before final verification, which becomes Wave 4): install efferd block via shadcn registry (`@efferd` added to `components.json`; deps: recharts+lucide-react already installed; `@radix-ui/react-avatar|separator|collapsible|dropdown-menu` only IF pulled imports use them), re-theme sales/billing → resume-analysis domain (KPI cards, score trend, section breakdown, recent analyses, empty state), per-user history persistence (`resume_analyses` table + RLS, **metrics + filename only — raw resume text NEVER stored**, privacy-maximal recorded default), auth-gated App-level `'landing' | 'dashboard'` view switch (NO router, zero new deps), Header "Dashboard" link when signed in. Privacy copy FINAL reconciliation in Todo 3.3 (guests zero-storage + signed-in saved-history wording). Commit sequence becomes feat ×9 / chore ×3 / docs ×1. Registry fetch VERIFIED 2026-08-14 (`https://efferd.com/r/dashboard-2.json` returns full block; page itself is JS-rendered).
- **pending action:** present updated plan brief for the dashboard scope change and await user's explicit okay before execution resumes (Todos 1.1–1.2 already DONE + committed `c1a1c87`, `75cb6c1`; Todo 1.3 readied, not started; Wave 2 needs the user's `sbp_` PAT placed in `.env.local` first).
- **2026-08-14 verification pass (user: "make sure all features are working and then proceed"):** all gates GREEN on committed state — typecheck 0, oxlint 0 (3 pre-existing warnings), vitest 57/57, build 0 (pre-existing chunk warning); Playwright smoke @ `127.0.0.1:8080`: title "ResumeLab — ATS Resume Analyser", hero H1 kinetic (7 `.will-change-transform` segments), footer cascade (118 `.cascade-front`), marquee "ATS" present, **0 console errors**. Todo 1.3 implementation decisions recorded in plan (FlippingWordSwap `onClick` prop + Header CTA swap; ScrollBasedVelocity strip `mt-8`). **Session is plan-mode restricted (`.omo/*.md` only — product edits AND subagent-routed implementation blocked by `[prometheus-md-only]` guard; ledger `.jsonl` write also blocked).** Execution requires the user to run `/start-work` in a fresh executor session — boulder.json active for `resume-analyser-ext`, plan decision-complete.
- **2026-08-14 review resolution:** user chose "Cancel stale reviews" — both background reviews (Momus bg_d869ddc6, Oracle bg_83fbee98) were launched against the PRE-Supabase plan and cancelled after 27+ min (file changed underneath them; Wave 2 findings would be stale). Wave 2 gap analysis done inline instead (secrets policy, rate-limit note, key-shape note, fallbacks). Plan is HR6-verified and decision-complete. Execution remains the user's call via /start-work.

## Request (2026-08-14)
User asked, on a COMPLETED ResumeLab project (all 12 todos done, Final Wave F1–F4 APPROVE, branch `feat/resume-analyser`):
1. "from componentry add make sure every section should have kinetic loading"
2. "create a login panel where they can login via phone number or email"

## Grounded facts (verified, not claims)
- **Componentry was promised but never integrated.** Original plan success criteria #4 + scope #2: "at least one Componentry animated component and at least one 21st.dev-sourced component (via magic-dev-21st MCP) integrated and restyled to the design tokens; provenance recorded in HANDOFF." HANDOFF.md line 55: "Componentry: not integrated (bash flaky during Todo 1.3; optional per plan — revisit if desired)."
- **Componentry = componentry.dev** — shadcn-registry animated component library (harshjdhv/componentry, 40+ components: Kinetic Text Reveal, Letter Cascade, Magnetic Dock, Particle Typography, Sticky Scroll Cards, Scroll Velocity, etc.). Install: `npx shadcn@latest add @componentry/<name>`. Built on React + Tailwind + Framer Motion. Project already has `motion` ^13.1.0 (Framer Motion's successor, API-compatible), Tailwind v4, `components.json` (shadcn registry, radix-nova style) → drop-in compatible.
- **Current kinetic coverage:** Header (motion.header entrance) ✓, Hero (SectionReveal + MagneticButton) ✓, SkillsMarquee (InfiniteSlider self-animating + ProgressiveBlur) ✓, ToolSection (SectionReveal ×2 + ScanSkeleton/AnalyzingSkeleton state treatments) ✓, HowItWorks (SectionReveal + StaggeredReveal) ✓, SampleReport (SectionReveal + counters) ✓, ReportView (ReportReveal stagger) ✓. **Footer.tsx = no animation at all** — the gap.
- **Design identity:** ink-on-paper light theme, Fraunces/Inter/IBM Plex Mono, emerald accent `oklch(0.58 0.13 162)`, radius 0.75rem. All motion respects `prefers-reduced-motion` (project-wide pattern). Success criteria require LCP < 2.5s, CLS < 0.1.
- **Login collision:** Plan TL;DR "What it will NOT do": *"No user accounts, no resume storage/database..."* Footer.tsx + ToolSection copy: *"Privacy first: your resume never leaves your browser. No uploads, no storage, no cookies."* Site is a **static Vite SPA, zero backend**, deployed via vercel.json (framework vite, outputDirectory dist, no serverless functions). Phone login requires an SMS provider (Twilio etc.); email login requires password storage or OTP/email provider — both need backend infra + keys + cost.
- **MCP state:** `magic-dev-21st` MCP is configured in opencode.jsonc (enabled) but NOT visible in this session's connected MCP resources (only websearch is). Executor must verify connectivity; Componentry primary path is the shadcn CLI registry, which needs no MCP.
- **Folder context:** `C:\Users\cloud\OneDrive\Desktop\Hybrid_Second_Brain\clients website\resume analyser` — a client-deliverable demo site.

## Open forks (answered 2026-08-14)
1. **Login architecture depth** → **UI demo panel** (user choice): polished login modal with email + phone tabs, OTP-style input flow, validation, mock authenticated state (in-memory React state only, ZERO persistence — localStorage forbidden). Keeps the "no uploads, no storage, no cookies" promise literally true. No backend, no keys, no cost.
2. **Kinetic motion intensity** → **Tasteful kinetics** (user choice): Componentry-sourced treatments restyled to ink-on-paper tokens; keep the restrained identity; Footer gets its missing animation; LCP < 2.5s / CLS < 0.1 preserved.

## Resolved decisions (for plan generation)
- **Componentry source:** componentry.dev shadcn registry — `npx shadcn@latest add @componentry/<name>`. Project already has `components.json` (radix-nova) + `motion` ^13.1.0 (Framer Motion successor) + Tailwind v4 → drop-in. MCP not needed (CLI registry); `magic-dev-21st` MCP was already fulfilled in Todo 1.3 (21st.dev sourcing done — InfiniteSlider/ProgressiveBlur/UploadCard/hover-footer).
- **Chosen Componentry components (tasteful tier, no WebGL/particle):**
  - `@componentry/kinetic-text-reveal` → Hero headline (word/char stagger, soft blur) + section headings (HowItWorks, SampleReport)
  - `@componentry/letter-cascade` → Footer brand + ToolSection heading
  - `@componentry/scroll-based-velocity` → SkillsMarquee strip / section divider (scroll-driven, light)
  - `@componentry/flipping-word-swap` → Header "Analyse" CTA micro-interaction (optional polish)
  - Install names verified from docs URLs (`/docs/components/kinetic-text-reveal` etc.)
- **Reduced-motion:** Componentry components built on Framer Motion may not ship reduced-motion guards — executor MUST audit each added component and add `useReducedMotion()` static fallbacks per project pattern (KineticLoader/SectionReveal precedent) before acceptance.
- **Login panel placement:** modal dialog from Header ("Sign in" button); after mock login, header shows masked identifier + Log out. Focus trap + Escape-to-close + focus restore (UploadZone paste-dialog precedent). Demo microcopy ("Demo — no account is created").
- **Tests:** TDD for validation logic (email regex, phone format) + component tests (tab switch, OTP flow, close). All 57 existing tests must stay green.
- **Privacy copy:** stays TRUE — demo login persists nothing. Add "Sign in (demo)" affordance without weakening the zero-upload promise.

## Plan waves (draft)
- Wave 1 (1.1–1.3): Componentry integration + per-section kinetic treatments + Footer gap + provenance
- Wave 2 (2.1–2.3): Login demo panel (foundation/validation → OTP flow + session → docs/privacy copy)
- Wave 3: Final verification wave F1–F4 (same protocol as original)

## Metis gap analysis (2026-08-14)
- Subagent dispatch billing-blocked (payment method required on opencode.ai workspace) → performed gap analysis inline as the critic against the plan text.
- CRITICAL — "every section" gap: todos 1.1–1.3 covered Hero/Footer/ToolSection/SkillsMarquee/Header but NOT HowItWorks/SampleReport → **added Todo 1.4** (KineticTextReveal on HowItWorks + SampleReport headings).
- HIGH — decision-complete violations: OTP input ("pick one"), session state ("context or App-level"), test file path unspecified → **pinned**: single 6-digit OTP input (`inputMode=numeric maxLength=6 pattern=[0-9]{6} aria-label`), App-level `useDemoAuth()` with prop-drilling (no context), `src/components/__tests__/LoginPanel.test.tsx` + `src/lib/auth-session.test.ts` with `// @vitest-environment jsdom` pragma.
- MEDIUM — Todo 3.1 commit line had leftover self-correction artifact → fixed to `chore: final verification wave evidence (ext F1-F4 approve)`.
- LOW — Header "Sign-in" must be visible on all breakpoints (outside the `hidden sm:flex` nav) → added to Todo 2.1 step 3.
- All findings folded in; plan re-verified (HR6: TL;DR leads, template headers in order, every todo has References + Acceptance + happy/failure QA with evidence paths + Commit line).

## Notepad / evidence ledger
- (filled during plan generation)
## 2026-08-14 SESSION NOTE: Todo 2.3 plan-vs-reality mismatch
VERIFIED: src/components/auth/LoginPanel.tsx, src/lib/auth-validation.ts, and any demo
sign-in UI do NOT exist in this repo (checked full tree + git log --all -- they were never
committed). The plan assumed an "original demo design" LoginPanel to rebuild. None exists.
DECISION: build LoginPanel + auth-validation FRESH per the plan's explicit spec (modal with
Email/Phone tabs, 6-digit OTP step, focus trap/Escape/focus-restore, inline validation,
aria-describedby, loading states, sonner toasts; all Supabase calls through src/lib/auth-flow.ts).
All acceptance criteria remain satisfiable; component tests (jsdom) cover open/close/Escape/
focus-restore/tabs/validation/OTP-step/mocked-submit. Real-browser OTP QA deferred to F3
(plan already allows this - needs a live inbox + provisioned project).
## 2026-08-15 SESSION NOTE: Todo 2.3 COMPLETE (commit pending)
- Built fresh: src/lib/auth-validation.ts, src/lib/auth-flow.ts, src/lib/__tests__/auth-flow.test.ts
  (11 tests), src/components/auth/LoginPanel.tsx, src/components/__tests__/LoginPanel.test.tsx
  (12 tests). Gates green: typecheck 0, lint 0 (3 pre-existing warnings), vitest 83/83, build 0.
- Playwright QA (scratch App swap, reverted before commit) caught a REAL bug the mocked unit
  tests missed: getSupabase() was evaluated as handle()'s argument, so a config-missing throw
  escaped as an uncaught PAGEERROR (sendResult empty, 1 console error). FIXED: handle() now
  takes a factory (() => Promise) so client acquisition happens INSIDE the try/catch; config
  missing maps to friendly "Sign-in isn't set up yet - add your Supabase keys to get started."
  Regression test added (supabaseConfig.throwOnGet toggle). Re-QA: 0 console errors, 0 page
  errors, graceful message on both email + phone tabs. Evidence .omo/evidence/ext-2-3-auth-flow.log
  + 4 screenshots (modal-open/invalid/otp/phone-tab). HANDOFF + plan checkbox updated.
- LESSON: mocked-module unit tests cannot catch synchronous throws in the setup path of a
  wrapped call - QA in the real browser (or a throw-on-get toggle in the test double) is
  required. The toggle is now part of the auth-flow test contract.
- Next: Todo 2.4 (session + header signed-in + HANDOFF/README) - session.ts/maskIdentifier
  TDD can proceed mocked; real-browser full flow still blocked on Todo 2.1 (.env.local sbp_ PAT).
## 2026-08-15 SESSION NOTE: Todo 2.4 COMPLETE (commit pending)
- src/lib/session.ts + __tests__/session.test.ts (12 tests): useAuthSession wraps
  onAuthStateChange -> {session,user,signOut}, Supabase default token storage (reload
  persistence); maskIdentifier built FRESH (plan ref "from demo design" stale - demo UI
  never existed); degrades to signed-out when .env.local missing (Todo 2.3 lesson:
  getSupabase() throw caught in effect).
- Header.tsx now {user,onSignIn,onSignOut}: signed-in = masked id + Log out; signed-out
  = Sign in - both OUTSIDE hidden sm:flex nav (all breakpoints). Header.test.tsx 6 tests.
- App.tsx mounts useAuthSession at App level + LoginPanel modal state opened by onSignIn.
- README + HANDOFF: "Sign in (Supabase Auth)" feature + setup section; HANDOFF new
  "Supabase Auth (Wave 2)" section (providers, SMS caveat, deploy note, privacy).
- Gates: typecheck 0, lint 0 (3 pre-existing), vitest 101/101, build 0. Playwright QA
  (real app): Sign in visible desktop + 375px mobile, modal opens Email/Phone tabs,
  Escape closes, 0 console errors (ext-2-4-signout.png + ext-2-4-session.log).
- Secrets gate: sbp_[A-Za-z0-9]{8,} -> 0 hits (no real token values; only masked
  doc references). Signed-in journey deferred to F3 (Todo 2.1 blocked).
- Next: Todo 2.1 provisioning BLOCKED (user sbp_ PAT into .env.local) OR Wave 3
  (3.1 efferd dashboard install can proceed - registry verified 2026-08-14).

- 2026-08-17: **Todo 3.1** - efferd dashboard-2 block installed + deps decision + tokens, TDD-n/a (install todo): components.json gained `@efferd` registry (URL deviation recorded - CLI requires `{style}/{name}`, plan said `{name}`; both styles serve identical JSON); `shadcn add @efferd/dashboard-2` succeeded - 40 files, Windows stray `@/` path bug hit again (same as Todo 1.1) and all files relocated to src/components + src/components/ui + src/hooks; NO @radix-ui/react-* individuals installed (consolidated `radix-ui` ^1.6.7 covers every pulled import - decision recorded); recharts pinned ^3.10.1 -> ^3.8.0 by efferd; shadcn semantic tokens + chart vars added to src/index.css (`@theme inline` aliases -> paper/ink/accent; `--color-muted` PRESERVED as the dark-gray text token, muted-FOREGROUND mapped to it instead; verified all semantic utilities + runtime vars present in built dist CSS); gates: typecheck 0 / lint 0 (4 benign warnings) / vitest 101/101 / build 0; FIXED a LATENT session.test.ts typing bug the tsbuildinfo cache had hidden at 06bd0ba (vi.fn zero-arg impl typed mock.calls as [][] - typed the callback param per real supabase-js signature); Playwright smoke of production build: landing renders fully, 0 console errors, no visual regression from new semantic utilities (call-site explicit classes + tailwind-merge dedupe). Evidence .omo/evidence/ext-3-1-install.log. Next: Todo 3.2 re-theme.
- 2026-08-17: **Todo 3.2** - dashboard re-themed to resume-analysis domain (content swap, layout/charts kept): 5 files renamed via git mv (net-revenue-chart->score-trend-chart, channel-sales-chart->section-breakdown-chart, dashboard-invoices->recent-analyses, billing-health->dashboard-empty, dashboard-activity->recent-activity) - deviation recorded (plan referenced efferd filenames; only dashboard.tsx imported them; F2 would flag sales-named components); KPI cards (Analyses run 12 / Average ATS score 71 / Skills detected 184 / Best score 78, placeholder until 3.3, mono labels); Score trend bar chart (last 7 analyses, CustomGradientBar + recharts kept, dataKey sales->score); Section breakdown line chart (keywords vs structure 0-100, two-line + glow kept); Recent analyses table (filename|format|score, table primitives unchanged); empty state (Empty primitives kept, icon FileTextIcon, ghost button -> #tool); activity list (analysis-completed events); DashboardCard rounded-none->rounded-xl (project radius); formater.ts docstring de-sales'd (acceptance grep 0 hits). Gates: typecheck 0 / lint 0 (4 benign) / vitest 101/101 (one forks-worker pool timeout flake - retry clean) / build 0. QA happy via scratch App swap (backup restored, git clean): all 4 KPI cards + 2 charts + table + empty state + activity render, 0 console errors, screenshot ext-3-2-retheme.png; reduced-motion emulation renders identically (static recharts). Evidence .omo/evidence/ext-3-2-retheme.log. Next: Todo 3.3 per-user history persistence (still blocked on Todo 2.1 provision - .env.local sbp_ PAT).- 2026-08-17: **Todo 3.3** - per-user analysis history persistence (client-side DONE; migration RUN + signed-in QA BLOCKED on Todo 2.1): TDD src/lib/history.test.ts (8 tests, mocked supabase client via makeClient()) -> src/lib/history.ts (saveAnalysis/loadHistory/deleteAnalysis, HistoryClient = Pick<SupabaseClient,'from'>, NEVER stores parsed.text - RAW RESUME TEXT IS NEVER STORED). ParsedResume gained ilename (additive, parsing pipeline only - NOT analyser-core). ToolSection new { user } prop: fire-and-forget saveAnalysis(getSupabase(), user.id, r, parsed) after setResult when signed in; failure -> sonner toast "Couldn't save this analysis - you're still signed out of history" (plan-exact); guests: no save/no toast/no console noise. App.tsx passes user. Privacy copy FINAL reconciled (Footer paragraph+meta, Hero stale claim, README 3/14/80/112, HANDOFF line 13): "Analyses run while signed in are saved to your account so you can review them in your dashboard. Your resume text is never stored. Sign out or delete your history any time."; grep: no stale claims remain. Migration script .omo/scripts/migrate-resume-analyses.mjs (gitignored, NEVER committed) - mechanism deviation recorded (plan said service_role key; supabase-js data API cannot run DDL; script uses Management API database/query with sbp_ PAT; SQL identical to plan schema). Gates: typecheck 0 / lint 0 (4 benign) / vitest 109/109 (15 files) / build 0. QA guest flow (Playwright dev :5201): signed-out analysis completes (ATS 100 Strong), 0 console errors, no save toast. Evidence ext-3-3-history.log. TODO 2.1 still BLOCKED (.env.local missing - needs user sbp_ PAT + VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY). Next: Todo 3.4 dashboard wiring (client-side unblocked) then surface PAT blocker for migration + signed-in QA.

## 2026-08-17 SESSION NOTE: Todo 3.4 COMPLETE (commit pending)

- **Todo 3.4** - auth-gated personal dashboard view (client-side DONE; signed-in QA BLOCKED on Todo 2.1): TDD src/lib/dashboard-data.ts (NEW, pure helpers: kpiStats - Analyses run / Average ATS score (delta first-to-last, 0-guarded) / Skills detected (union) / Best score, truthful footers "vs first analysis"/"all time"; scoreTrend - last 7 oldest-to-newest; sectionBreakdown - last 7 normalized 0-100 via WEIGHTS (keywords 45/structure 17), date-only strings so formatDate/parseIsoCalendarDate never see Invalid Date; recentRows - newest-first format uppercased; activityItems - "Analysed {filename}" + date-only) + src/lib/__tests__/dashboard-data.test.ts (15 tests). Dashboard components wired to real rows: stats.tsx, score-trend-chart.tsx, section-breakdown-chart.tsx, recent-analyses.tsx ("View All" dead link -> "Back to analyser" button -> onNavigate('landing')), recent-activity.tsx (formatDate "full"), dashboard-empty.tsx ("Analyse a resume" -> onNavigate('landing')). src/components/dashboard.tsx = Dashboard({ userId, onNavigate }) - loadHistory(getSupabase(), userId) in useEffect, getSupabase() wrapped in try/catch (throws without .env.local - Todo 2.3 lesson; degrades to error state), loading/error/empty (DashboardEmpty full-width)/data states. Header.tsx gained OPTIONAL view/onNavigate props (defaults keep existing consumers/tests valid) - signed-in "Dashboard" link next to Analyse CTA (same pattern as Sign in, aria-current="page" when active); nav links + Analyse CTA + logo switch back to landing via goToSection (rAF scroll after view swap). App.tsx = view state ('landing' default) + effect redirecting signed-out users away from 'dashboard' + render guard view === 'dashboard' && user (no dashboard flash with no user). Header.test.tsx +4 tests. Gates: typecheck 0 / lint 0 (4 benign) / vitest 128/128 (16 files) / build 0. QA guest path (Playwright dev :5201): signed out -> header shows Sign in + Analyse only, NO Dashboard link (only "Dashboard" text on page is Footer privacy copy), landing renders, 0 console errors; screenshot ext-3-4-signedout-header.png; evidence ext-3-4-gate.log. Signed-in happy path DEFERRED (dashboard with real KPI/charts/table + empty-history empty-state render need Todo 2.1 provision). TODO 2.1 still BLOCKED (.env.local missing - needs user sbp_ PAT + VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY for migration RUN + signed-in QA 3.3/3.4 + Final Wave F3). Next: surface PAT blocker to user, then Wave 4 F1-F4 verification.
